camera
{
	origin(0 0 25)
	direction(0 0 -1)
}
content
{
	object(light)
	{
		color(1 1 1)
		origin(100 100 100)
		intensity(50000)
	}
	object(sphere)					# Left
	{
		origin(-50  0 -100)
		radius(20)
		mat(redplastic)
	}
	object(sphere)					# Middle
	{
		origin(0 0 -100)
		radius(20)
		mat(whiteplastic)
	}
	object(sphere)					# Right
	{
		origin(50 0 -100)
		radius(20)
		mat(blueplastic)
	}
}
