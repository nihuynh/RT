os:
- linux
- osx
language: c
sudo: required
addons:
  homebrew:
    packages:
    - sdl2
install:
  - if [ $TRAVIS_OS_NAME == "linux" ]; then
    sudo install_sdl.sh;
    fi
script: make

# Notifications :
matrix:
  allow_failures:
    - os: linux
notifications:
  slack: 42born2code:6JQ1d0KBj9Vggul0KNL9p0fW
